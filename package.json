{
  "name": "@emtux/actions",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mutex-inc/emtux-actions.git"
  },
  "author": "mutex inc.",
  "license": "PRIVATE",
  "scripts": {
    "clear": "rm -rf dist",
    "compile": "npm run eslint-fix && npm run clear && npx tsc",
    "update-version": "npm version patch && git push --tags && git push origin",
    "pub": "npm run compile && npm run update-version && npm publish",
    "eslint": "eslint --cache --config .eslintrc.json --ext .ts",
    "eslint-fix": "eslint --cache --config .eslintrc.json --fix  --ext .ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "@types/node": "^17.0.21",
    "@typescript-eslint/eslint-plugin": "^4.14.1",
    "@typescript-eslint/experimental-utils": "^5.14.0",
    "@typescript-eslint/parser": "^5.14.0",
    "eslint": "^7.23.0",
    "eslint-config-prettier": "^6.10.0",
    "eslint-plugin-import": "^2.20.1",
    "eslint-plugin-prettier": "^3.1.2",
    "jest": "^27.5.1",
    "prettier": "^2.2.1",
    "ts-jest": "^27.1.3",
    "ts-node": "^10.5.0",
    "typescript": "^4.2.4"
  }
}